import core.Server;

public class Main {
    public static void main(String[] args) {
        // Server server = new Server(1234);
        Server server = new Server();
        server.launch();
    }
}
